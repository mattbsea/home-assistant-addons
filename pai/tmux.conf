# PAI add-on tmux configuration.
# Keeps the Claude Code session alive while the browser is disconnected.
set -g status off
set -g mouse on
set -g history-limit 20000
set -g default-terminal "xterm-256color"
set -g destroy-unattached off
